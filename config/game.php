<?php

/**
 * Konfiguracja gry Cat Survival.
 *
 * Wartości pobierane z .env z domyślnymi fallbackami.
 */
return [

    /*
    |--------------------------------------------------------------------------
    | Wersja formatu save'ów
    |--------------------------------------------------------------------------
    | Zmiana tej wartości oznacza niekompatybilność z poprzednimi save'ami.
    */
    'save_version' => (int) env('GAME_SAVE_VERSION', 1),

    /*
    |--------------------------------------------------------------------------
    | Multiplayer — tick rate serwera
    |--------------------------------------------------------------------------
    | Ile razy na sekundę serwer aktualizuje stan gry (ticków/s).
    */
    'tick_rate' => (int) env('GAME_TICK_RATE', 20),

    /*
    |--------------------------------------------------------------------------
    | Autosave — domyślny interwał (sekundy)
    |--------------------------------------------------------------------------
    */
    'autosave_interval' => (int) env('GAME_AUTOSAVE_INTERVAL', 60),

    /*
    |--------------------------------------------------------------------------
    | Multiplayer — max graczy per pokój
    |--------------------------------------------------------------------------
    */
    'max_players_per_room' => (int) env('GAME_MAX_PLAYERS_PER_ROOM', 10),

    /*
    |--------------------------------------------------------------------------
    | Save — max slotów ręcznych per user
    |--------------------------------------------------------------------------
    */
    'max_save_slots' => 3,

    /*
    |--------------------------------------------------------------------------
    | Dostępne terytoria (biomy)
    |--------------------------------------------------------------------------
    */
    'territories' => [
        'pine_forest',
        'deciduous_forest',
        'desert',
        'mountains',
        'swamp',
    ],

    /*
    |--------------------------------------------------------------------------
    | Dostępne wzory futra kota
    |--------------------------------------------------------------------------
    */
    'cat_patterns' => [
        'solid',
        'striped',
        'spotted',
    ],

    /*
    |--------------------------------------------------------------------------
    | Dostępne kategorie wieku kota
    |--------------------------------------------------------------------------
    */
    'cat_ages' => [
        'young',
        'adult',
        'senior',
    ],

    /*
    |--------------------------------------------------------------------------
    | Dostępne płcie kota
    |--------------------------------------------------------------------------
    */
    'cat_genders' => [
        'male',
        'female',
    ],

];
